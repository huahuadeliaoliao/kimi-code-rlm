---
"@moonshot-ai/kimi-code": minor
---

Make the local build use a single persistent Python RLM harness with a lean single-role prompt, compact output handles, structured process-task control, retries for transient upstream HTTP 499 and 524 responses, and runtime-managed Goal work slices that avoid model-generated progress chatter.
